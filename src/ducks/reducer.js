import axios from 'axios'

const initialState = {
	searchMovie: [],
	selectedMovie: null
}


export default function reducer(state = initialState, action) {
	switch(action.type) {
		default: 
			return state;
	}
}